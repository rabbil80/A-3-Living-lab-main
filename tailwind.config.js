/* @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.{html,js}'], // Specify the files to scan for class names
    theme: {
      extend: {}, 
    },
    plugins: [], 
  };
  